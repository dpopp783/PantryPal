#from flask_wtf import FlaskForm
#from wtforms import (
 #   StringField,
  #  DecimalField,
 #   DateField
##)
##from wtforms.validators import InputRequired, Length
#from config import MAX_INGREDIENT_NAME_LENGTH


#class AddInventoryEntry(FlaskForm):
  #  ingredient_name = StringField(validators=[InputRequired(), Length(1, MAX_INGREDIENT_NAME_LENGTH)])
   # quantity = DecimalField(validators=[InputRequired()])
   # unit = StringField(validators=[InputRequired()])
    ##exp_date = DateField(validators=[InputRequired()])


#class AddShoppingListEntry(FlaskForm):
   # ingredient_name = StringField(validators=[InputRequired(), Length(1, MAX_INGREDIENT_NAME_LENGTH)])
    #quantity = DecimalField(validators=[InputRequired()])
    #unit = StringField(validators=[InputRequired()])